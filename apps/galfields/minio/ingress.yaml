# Expone solo la API S3 (para servir/subir PDFs e imágenes). La consola de
# administración (9001) queda interna; usar port-forward para acceder a ella.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minio
  namespace: galfields
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.middlewares: galfields-minio-bucket-prefix@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - cdn.galfields.kinforgeworks.com
  rules:
    - host: cdn.galfields.kinforgeworks.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: minio
                port:
                  number: 9000
